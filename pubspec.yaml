name: hoosierciv
description: HoosierCiv — Indiana civic engagement app
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.6
  supabase_flutter: ^2.5.6
  firebase_core: ^3.3.0
  firebase_messaging: ^15.1.0
  firebase_analytics: ^11.3.0
  go_router: ^14.2.7
  hive_flutter: ^1.1.0
  tflite_flutter: ^0.10.4
  exif: ^3.3.0
  share_plus: ^10.0.2
  cached_network_image: ^3.4.1
  url_launcher: ^6.3.1
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.12
  flutter_lints: ^4.0.0
  bloc_test: ^9.1.7
  mocktail: ^1.0.4

flutter:
  uses-material-design: true
